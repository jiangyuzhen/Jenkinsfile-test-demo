pipeline{
    stages {
        stage('Install Packages') {
            steps {
             echo 'Install Packages'
            }
        }

        stage('test and build') {
            steps {
             echo 'test and build'
            }
        }

        stage('deployment') {
            steps {
             echo 'waiting to deployment'
            }
        }
    }
}